# ESTADO DEL PROYECTO

## Idea validada (RESUMEN FINAL pegado por el usuario — 2026-09-22)

- **Nombre tentativo:** SleepPayoff (alternativas: SleepDebt, RestCalculator)
- **En una frase:** Calculadora y planificador de "deuda de sueño" que muestra a trabajadores y freelancers cuánta energía pierden y cómo recuperarla en 3 días, sin falsas promesas.
- **Problema:** la gente se despierta agotada, rinde mal en el trabajo y no sabe cuántas horas de sueño debe ni cómo recuperarse sin alterar su rutina laboral.
- **Cliente ideal:** freelancers y profesionales remotos de alto rendimiento, 25-45 años, cansancio acumulado. Pagan $5.99-$6.99 USD/mes. Avatar: "Carlos", 32, developer remoto.
- **Campo 5 — dolores reales (textuales, usar para copy):**
  - "I don't need another app telling me I slept poorly, I need to know how to fix my sleep debt practically."
  - "Sleep tracking is a giant gimmick that just gives you a random score without actionable steps."
  - "All these apps push long expensive subscriptions before showing if the calculation is even accurate for my schedule."
- **Campo 11 — funciones núcleo v1 (construir):**
  1. Calculadora rápida de deuda de sueño acumulada.
  2. Generador de plan de recuperación de 3 días (siestas estratégicas + hora de desconexión).
  3. Indicador de impacto en productividad/energía diaria.
  - **NO construir todavía:** grabador de ronquidos, catálogo de música/meditaciones, sincronización con wearables.
- **Campo 15 — precio y modelo:** **$5.00/mes o $36.00/año ($3.00/mes, ahorra 40%)** — ajustado por el usuario 2026-09-22 (el RESUMEN FINAL original proponía $6.99/$39.99). **2 días de prueba gratis** (también ajustado, original proponía 7). Pasarela: Stripe.
- **Campo 16 — costo por cliente:** ~$0.05 USD/mes infra/IA. Margen ~99%.
- **Campo 18 — las 3 tomas del video (30s):**
  1. 0s: "Deuda de sueño: 8.5 horas. Tu cerebro está funcionando al 60%."
  2. 10s: usuario desliza barra de horas dormidas.
  3. 25s: plan claro — "Desconéctate a las 10:15 PM, siesta de 20 min a las 2:00 PM."
- **Campo 20 — ventaja del usuario:** enfoque de cero falsas promesas — no vende "curar el insomnio", sino una herramienta cuantitativa y honesta.
- **Competencia:** Rise Science ($59.99/año, caro/complejo), Sleep Cycle (falla el tracking por micrófono), ShutEye (audios + subs agresivas). 8 apps parecidas 🟢 (mercado poco saturado). Facturación categoría: +$1M-$2M/mes.
- **Diferenciador:** "La única app que traduce tu deuda de sueño en un plan claro de recuperación de 3 días para no perder productividad."
- **Propuesta de valor ganadora (v1, enfoque productividad/dinero):** "Ayudo a freelancers y trabajadores remotos a eliminar la neblina mental y recuperar su claridad de trabajo en 3 días sin comprar relojes inteligentes caros ni perder tiempo en meditaciones guiadas."
- **3 razones de compra dominantes:** escapar del dolor mental/físico (neblina mental, culpa) → ahorrar tiempo/evitar esfuerzo (30s/día, instrucción exacta) → ganar dinero vía productividad.
- **Objeciones a resolver en landing/onboarding:** "otra app de sueño inservible", "no quiero reloj inteligente", "es una suscripción más", "mi horario es caótico".
- **Lenguaje del cliente (para copy):** "cabeza con neblina", "modo zombi", "no sé cómo pagar la deuda de sueño que traigo encima".
- **Canales:** TikTok Ads, Instagram Reels, contenido orgánico freelance/remote work.

## Etapa actual

**Paso 1 (landing) → LISTA** (11ª pasada del revisor-visual: 37/40 usabilidad, 16/20 craft, 19/20 copy — cruzó los 3 umbrales).

**Paso 2 (onboarding) → NO LISTA, EN PAUSA.** El código de `/onboarding` existe, compila y se puede abrir en el navegador para probarlo, pero el gate de calidad NO se cumplió: 10 pasadas del revisor-visual, se quedó en 31/40 usabilidad y 15/20 craft (umbral 36/16 — ver "Problemas conocidos" → veredicto:onboarding). NO se declara terminado ni listo. El pulido se pausó por acuerdo explícito del usuario 2026-09-23 para avanzar al Paso 3; retomar el pulido del onboarding sigue pendiente.

Notas de contenido (para retomar el pulido): flujo de 11 pasos — reordenado y ampliado tras comparar con una propuesta externa (Gemini) que el usuario trajo 2026-09-23: meta (primero, "perceived fit") → neblina (dolor) → reconocimiento → objeción dominante ("¿ya probaste algo?") → hora de despertar (ancla horaria) → café (eco literal de VoC de FICHA-AVATAR "3 o 4 cafés al día") → promedio de horas dormidas de la última semana (más honesto que "anoche") [slider] → hora de desconexión [slider, commitment device] → reconocimiento con etiquetado positivo → loading animado → resultado real calculado con los datos del usuario. Se descartaron de la propuesta externa: una métrica inventada ("62% de capacidad mental") y el precio/trial de la competencia ($6.99, 7 días — ya decidido $5/$3, 2 días). CTA final lleva a `/paywall` (placeholder — Paso 3 aún no construido).

**Paso 3 (paywall) → LISTA** (5ª pasada del revisor-visual: 36/40 usabilidad, 16/20 craft, 19/20 copy — cruzó los 3 umbrales, docs/revisiones/paywall-veredicto.md). `/paywall` reemplaza el placeholder: headline con la cifra real de deuda del usuario (conteo animado), timeline del trial de 2 días (Hoy/Día 1 aviso por notificación/Día 2 cobro con fechas y monto reales), cards Anual ($3/mes, $36/año, badge "AHORRA 40%") y Mensual ($5/mes) con entrada escalonada, garantía nombrada bajo el CTA, manejo de error visible en el botón de compra, CTA que simula el flujo (Stripe se conecta recién en el Paso 6 — mockup honesto, sin checkout falso), cierre X, "Ahora no" y "Ya compré, iniciar sesión" (adaptación de "restaurar compra" al modelo Stripe/web). El onboarding pasa `deuda`/`desconexion`/`puntos`/`respuestas` por query params al paywall. tsc ✓ build ✓.

**Paso 4 (login/auth) → NO LISTA, EN PAUSA.** `/entrar` reemplaza el placeholder: flujo de 2 pasos (correo → código de 6 dígitos), sin contraseña (magic link/OTP — método decidido en "Decisiones técnicas" abajo), botón de Google marcado honestamente "Pronto" (OAuth real llega en el Paso 6), CTA siempre activo con validación inline + sacudida en vez de deshabilitado, spinner de carga, manejo de error real (código "000000" simula un código inválido), botón X para salir, entrada escalonada. tsc ✓ build ✓. 4 pasadas del revisor-visual (docs/revisiones/entrar-veredicto.md): usabilidad 27→28→31→30/40 (umbral ≥36, nunca convergió), craft 13→13→14→14/20 (umbral ≥16). Pulido pausado por acuerdo EXPLÍCITO del usuario 2026-09-23 (mismo patrón que el onboarding) para avanzar al Paso 5. `/app` creado como placeholder honesto de destino tras el login (Paso 5 aún no construido).

**Paso 5 (app interna) → construida, EN PAUSA.** 4 secciones con nav inferior (`/app` Hoy, `/app/historial`, `/app/semana`, `/app/cuenta`), cada una con protagonista propio (hoy: registro del día · historial: noches pasadas · semana: patrón/insight semanal · cuenta: plan y ajustes), datos en localStorage con semilla realista de 5 noches (Supabase real en el Paso 6 — SECUENCIA-MAESTRA §4 permite datos locales antes de conectar servicios). LOOP de retención definido (ver Decisiones técnicas abajo). "Hoy" es la pantalla principal (M0, una de las 4 del dinero): 6 pasadas del revisor-visual, usabilidad 29→32→29→34→35→31/40 (umbral ≥36, nunca convergió — mismo patrón de variancia que onboarding/login), craft 12→14→13→15→15→14/20 (umbral ≥16). Pulido pausado por acuerdo EXPLÍCITO del usuario 2026-09-23. Historial/Semana/Cuenta son pantallas secundarias (medición + checklist, sin revisor obligatorio). tsc ✓ build ✓ en las 4 rutas.

**Paso 6 (servicios externos) → EN PROGRESO, 2026-09-24.**
1. **Git/GitHub → HECHO.** Repo privado `sebastianbarrios070-cloud/SleepPayoff`. 2 commits del trabajo de esta sesión (paywall+login, app interna) + push inicial. `git remote origin` apunta a GitHub, rama `master` rastreada.
2. **Supabase → HECHO.** Proyecto `sleeppayoff` (ref `shmnrojfbuczdzekkhtv`, región us-east-1). Tablas `perfiles` (1 fila por usuario, hora_desconexion/meta/plan) y `noches` (fecha/horas, unique por usuario+fecha) con RLS activo en ambas (`auth.uid() = id/user_id`, patrón `(select auth.uid())` de 26). Trigger que crea el perfil automáticamente al confirmarse un usuario nuevo (función restringida a `security definer` sin EXECUTE público — advisor de seguridad limpio salvo un hallazgo benigno de la propia plataforma, `rls_auto_enable`, no creado por nosotros). Auth real conectada: `/entrar` usa `signInWithOtp`/`verifyOtp` de verdad (ya no es mock). Cliente de navegador/servidor + middleware (`proxy.ts`, Next 16 renombró `middleware.ts`) que protege `/app` y refresca la sesión. **Login probado end-to-end y funcionando en producción** (correo real → código real → entra a `/app`). Detalle no obvio: el token OTP de Supabase resultó ser de **8 dígitos, no 6** — el `CodeInput` se descubrió mal dimensionado recién al probar con el correo real y se corrigió (antes asumía 6 por la doc genérica de 26-AUTH-MODERNO, sin verificarlo contra el comportamiento real del proveedor).
3. **Vercel → HECHO.** Cuenta conectada a GitHub, proyecto `sleep-payoff` importado y enlazado al repo (auto-deploy activado, verificado con 2 pushes seguidos desplegando solos). Variables de entorno `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`/`RESEND_API_KEY` configuradas en production/preview/development. **App publicada y viva en https://sleep-payoff.vercel.app** (landing real, login real, no placeholders). Nota técnica: el primer intento de deploy falló (404) porque el directorio raíz no quedó apuntando a `sleeppayoff/` — se corrigió vía API y un nuevo push disparó el redeploy correcto; la API de Vercel bloqueó (403) que el propio agente creara un deployment de producción directo, que es el comportamiento correcto según la doctrina de `62` ("el agente jamás posee credenciales de escritura sobre producción") — el redeploy se hizo por el flujo normal de git push.
4. **Resend → HECHO (modo sandbox).** Cuenta creada, API key guardada en Vercel (`RESEND_API_KEY`, secreta) y configurada como SMTP personalizado en Supabase (host `smtp.resend.com`, puerto 465, remitente `onboarding@resend.dev` — dominio compartido de prueba de Resend). La plantilla de Magic Link de Supabase ahora incluye el combo enlace + código de 6→8 dígitos (`{{ .Token }}`), verificado recibiendo el correo real. **Límite actual:** sin dominio propio verificado, Resend solo entrega al correo del dueño de la cuenta — para mandarle correos a clientes reales hace falta el dominio (siguiente paso) y volver a apuntar el remitente a una dirección de ese dominio (ej. `no-reply@sleeppayoff.com`).
5. **Dominio → PENDIENTE.** El usuario no tiene uno aún; es lo siguiente.
6. **Stripe real → PENDIENTE** (el paywall sigue en modo simulado).

Próximo paso: comprar el dominio, apuntarlo a Vercel, actualizar el remitente de Resend y las URLs de Supabase Auth a la URL final; después conectar Stripe real.

## Registro anti-repetición (para no reciclar en el próximo proyecto de este usuario)
- Nombre elegido: SleepPayoff.
- Paleta/tipografía: oscuro casi-negro + degradé naranja→violeta (de RISE) + verde menta como 2ª nota · Space Grotesk (display) + Inter (body). Vetados para el próximo proyecto — ver [FICHA-ARTE.md](FICHA-ARTE.md).

## Decisiones tomadas (con evidencia del SO)

- **App modelo elegida: RISE: Sleep Tracker (Rise Science).** Revenue probado por 2 señales independientes (Sensor Tower ~$400k/mes marzo 2026 + top grossing #50 Health&Fitness US oct 2024). Plano completo, quejas y ángulo de diferenciación en [FICHA-MODELO.md](FICHA-MODELO.md). Nuestro eje: registro 100% manual (sin depender de wearables — su queja #1) + enfoque en productividad laboral para freelancers, en vez de salud general.
- **Avatar aprobado por el usuario: Carlos, 32, freelancer/developer remoto.** Ficha completa con 13 frases VoC, dolores/deseos en 3 niveles, 6 objeciones con respuesta/destino, diagnóstico de consciencia (consciente del problema, soluciones ya probadas fallaron) y sofisticación (mercado quemado — ángulo del hero: mecanismo al frente, no promesa genérica). Ver [FICHA-AVATAR.md](FICHA-AVATAR.md).
- **Identidad visual: fusión de líderes (usuario delegó la decisión).** Modo oscuro + degradé naranja→violeta de RISE (líder #1/modelo) + tratamiento de cards de Fabulous + disciplina de acento de Headspace + anti-sobre-gamificación de Linear. Dispositivo ownable propio: línea de horizonte degradé. Detalle completo en [FICHA-ARTE.md](FICHA-ARTE.md).

### Decisiones técnicas (internas — no se le preguntaron al usuario, ver "PREGUNTAR vs DECIDIR" de CLAUDE.md)

- **Método de auth: magic link/OTP por email como primario (Supabase Auth cuando se conecte en el Paso 6), sin contraseña.** Combo enlace+código de 6 dígitos en el mismo correo (jerarquía de `docs/sistema/26-AUTH-MODERNO.md`). "Continuar con Google" como atajo secundario, visualmente marcado "Pronto" hasta que el Paso 6 conecte el proveedor real — nunca simula un login que no ocurre. Passkeys quedan como mejora posterior (se ofrecen tras la primera victoria en la app, no en el primer login). Sesión larga (30-90 días) por ser app de consumo/hábito, no B2B/dinero sensible.
- **Loop de retención (24-GAMIFICACION, test "si borro tu historial, ¿la app de mañana es idéntica?" → pasa: no, cambia el insight y la deuda):** GATILLO = notificación a la hora habitual de despertar del usuario (dato ya capturado en el onboarding), pidiendo el registro de anoche · ACCIÓN central (1 tap/slider) = registrar horas dormidas, en la pantalla "Hoy" (M0) · RECOMPENSA VARIABLE = deuda de sueño actualizada + insight nuevo calculado del historial real (nunca genérico) + racha visible · INVERSIÓN = historial de noches que afina el promedio/insight y la hora de desconexión — el registro de hoy cambia lo que la app dice mañana. Racha simple implementada (sin streak-freeze todavía — v2, documentado como fuera de alcance del MVP). Modelo de datos v1: localStorage versionado (`lib/sleepData.ts`), migra a Supabase con RLS en el Paso 6.

## Problemas conocidos

- FICHA-MODELO.md §6 (ángulos de ads): no se pudieron obtener los 5 ads individuales de Meta Ads Library con fecha de inicio exacta (sin navegador autenticado en esta sesión). Se documentaron datos agregados (formatos, volumen semanal) con fuente y fecha. Revisar con acceso a Meta Ads Library antes de definir los ads propios en la etapa `34-ADQUISICION`.
- ~~**vista-previa**: `vista-previa-app.html` ausente~~ — RESUELTO: usuario eligió Opción A, aprobó el tour tras 2 ajustes. FICHA-ARTE.md cerrada (aprobada: SÍ).
- **docs/copy**: páginas legales son un BORRADOR redactado por el agente (47-LEGAL-FISCAL-Y-PRIVACIDAD.md permite esto para no bloquear la landing), no revisado por un abogado. ⚠️ Recomendado antes de vender a escala: revisión legal local (México).
- **FICHA-MERCADO**: no se creó una ficha dedicada — la investigación de mercado vive en ESTADO.md (RESUMEN FINAL pegado por el usuario) y en FICHA-MODELO.md/FICHA-AVATAR.md, que ya la citan con fuentes. Se documenta acá por si un gate futuro la busca como archivo aparte.
- ~~**veredicto:landing**~~ — RESUELTO 2026-09-23: 11 pasadas del revisor-visual, veredicto final **LISTA** (37/40 usabilidad, 16/20 craft, 19/20 copy — docs/revisiones/landing-veredicto.md). Progreso de usabilidad: 9→24→26→28→31→33→32→29→27→37. Fixes clave de las últimas rondas: CTA principal pasado a degradé naranja→violeta (FICHA-ARTE lo exigía y solo vivía en los mocks, no en el componente real), Kicker desaturado a `--text-tertiary` (disciplina 60-30-10), `focus-visible` global en `app/globals.css` para todo `<a>`/`<button>`, ruta `/entrar` creada (antes 404), `app/not-found.tsx` agregado, y el `?plan=` de Oferta conectado hasta `/paywall` vía el onboarding. Craft y copy habían cruzado su umbral desde la ronda 8-9; usabilidad fue la última en converger tras corregir el 404 real de "Entrar".
- **veredicto:onboarding** — 10 pasadas del revisor-visual (docs/revisiones/onboarding-veredicto.md tiene la 10ª). Usabilidad: 27→27→32→28→27→30→32→28→31 sobre 40 (umbral ≥36, nunca convergió — variancia real entre instancias del revisor, confirmada explícitamente por el revisor mismo en la 10ª pasada). Craft: 12→12→14→13→14→14→15→13→15/20 (umbral ≥16, quedó a 1 punto). Copy N/A (no vende). Se pausó el pulido tras 10 rondas por acuerdo EXPLÍCITO con el usuario (preguntó "cuánto más tardará", se le propuso 1 ronda final de 2 fixes concretos y parar después — así se hizo). Fixes reales aplicados en el camino: causa raíz de los huecos verticales (FooterCta usaba `mt-auto`, corregido a flujo normal + `justify-center` en el padre — resuelto y confirmado en 3 pasadas seguidas), línea de horizonte en las 11 pantallas, stagger de entrada en chips, chips de valor rápido bajo los sliders (44px), ícono Lucide en vez de emoji, re-tap permitido durante la pausa de selección, selección visual persiste al volver atrás, degradé del número héroe corregido (90deg con stops explícitos, antes se veía magenta apagado), sistema de puntos (+10 por respuesta, pedido explícito del usuario) anclado a "precisión de tu perfil" en el resultado, y favicon reemplazado por la marca (antes el diamante default de Next.js). Defectos que quedaron sin resolver, todos de pulido fino según el propio revisor (no fallas que un usuario note sin buscarlas): el `<input type="range">` nativo no usa el degradé del kit, el botón "Salir" bajo 44px de área táctil, el número "4.2h" sin explicar su método de cálculo, y el badge de puntos solo se explica al final del flujo. ⚠️ La app modelo (`ResultStep.tsx`) usa la frase "Sin fases REM" en su copy — VETADA por FICHA-AVATAR.md §Lenguaje. No se corrigió porque quedó fuera del alcance de esta sesión (se detectó al revisar el paywall); pendiente para la próxima ronda de pulido del onboarding.
- ~~**veredicto:paywall**~~ — RESUELTO 2026-09-23: 5 pasadas del revisor-visual, veredicto final **LISTA** (36/40 usabilidad, 16/20 craft, 19/20 copy — docs/revisiones/paywall-veredicto.md). Progreso de usabilidad: 28→29→30→35→36. Craft: 12→14→13→16→16. Copy: 15→19→19→19→19. Fixes clave: ícono del nodo "Día 1" del timeline (usaba el mismo check que "Hoy", parecía ya ocurrido — corregido a punto distinto), choque de números en headline/CTA ("3 días" vs "2 días" — headline cambiado a la cifra real de deuda del usuario), ancla emocional del dolor #1 fusionada en el subcopy, conteo de "respuestas" real por query param (antes aproximado desde el sistema de puntos), cifra héroe con conteo animado, garantía nombrada explícita bajo el CTA, manejo de error visible en el CTA de compra, canal del aviso Día 1 aclarado ("por notificación"), stagger de entrada en las plan cards, línea de horizonte engrosada, y remoción de un bloque (ValueStack) que se cortaba a media frase en el viewport de 812px — su contenido más fuerte se movió al trust row siempre visible. Corregido también un uso de "fases REM" (frase vetada por FICHA-AVATAR) que se había copiado sin querer del patrón del onboarding. Deuda de pulido menor (no bloqueante, anotada por el revisor): el value-stack de "qué incluye" no se reintrodujo tras quitarlo (el trust row lo cubre parcialmente), la selección de plan no persiste si el usuario sale y vuelve, y el try/catch de `comenzar()` es un mock sin fallo real posible hasta conectar Stripe en el Paso 6.
- **veredicto:app-hoy** — 6 pasadas del revisor-visual (docs/revisiones/app-hoy-veredicto.md tiene la 6ª). Usabilidad: 29→32→29→34→35→31/40 (umbral ≥36, nunca convergió — mismo patrón de variancia entre instancias del revisor ya visto en onboarding). Craft: 12→14→13→15→15→14/20 (umbral ≥16). Copy N/A (no vende). Pulido pausado tras 6 rondas por acuerdo EXPLÍCITO del usuario 2026-09-23. Fixes reales aplicados en el camino: botón "Editar"/"Cancelar" en el registro del día (antes no había forma de corregir), bug real de racha corregido (no contaba el día de hoy tras registrarlo), aviso + "Reintentar" si falla el guardado en localStorage, slider fino (3-10h, paso 0.5) reemplazando los chips como único método (antes solo 4-5 valores redondeados), thumb/track del slider rediseñado a mano (antes control nativo del navegador), token `--error` creado y aplicado en TODA la app (antes hex hardcodeado en 4 archivos), sombras tintadas (`--shadow-1`/`--shadow-2`) en las 4 cards, superficie hundida real con `--surface-2` detrás del slider, transición entre pestañas del nav inferior, transición del swap formulario↔confirmación, glow de hito en la racha (7/14/30 días). Defecto que quedó sin resolver (el de más impacto en la última pasada): el estado "ya registrado hoy" deja un vacío muerto notable bajo la card de desconexión — pendiente para la próxima ronda de pulido.
- **Prueba social en el paywall**: pendiente a propósito. El usuario pegó una guía externa de paywall 2026-09-23 que incluía un testimonio de ejemplo ("David M."); se le explicó que la app no tiene compradores reales todavía y que inventar una reseña viola la regla del SO de "cero testimonios fabricados" — confirmó no inventarlo. El resto de la guía ya estaba implementado (espejo del dato real, plan anual por defecto con ahorro real de 40%, timeline de 2 días, CTA sin fricción). Retomar la prueba social cuando existan compradores reales (después del Paso 6/lanzamiento).
